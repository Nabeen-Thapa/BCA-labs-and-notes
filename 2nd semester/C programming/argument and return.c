//function with argument and with return
#include<stdio.h>
int area(int,int,int)
int main();
{
	int p,t,r,s;
	printf("enter the values for p,t,r: ");
	scanf("%d%d%d",&p,&t,&r);
	s=si();
	printf("total=%d",s);
	return 0;
}
int area(int_p,int_t,int_r)
{
	int total;
	total=p*t*r/100;
	return total;
}
